# Environment Variables for GCP Deployment
# Copy this to .env.production and fill in your values

# ============================================
# DATABASE CONFIGURATION
# ============================================
# For Cloud SQL, use Unix socket connection:
DATABASE_URL=postgresql+pg8000://solacesquad_user:YOUR_DB_PASSWORD@/solacesquad?unix_sock=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME/.s.PGSQL.5432

# Example:
# DATABASE_URL=postgresql+pg8000://solacesquad_user:mypassword123@/solacesquad?unix_sock=/cloudsql/solacesquad-prod:us-central1:solacesquad-db/.s.PGSQL.5432

# ============================================
# SESSION & SECURITY
# ============================================
# Generate a random secret key (use: openssl rand -hex 32)
SECRET_KEY=your-super-secret-key-minimum-32-characters-long

# ============================================
# GCP CONFIGURATION
# ============================================
# Your GCP project ID
GCP_PROJECT_ID=solacesquad-prod

# GCP region for Vertex AI
GCP_LOCATION=us-central1

# ============================================
# FIREBASE (OTP Authentication)
# ============================================
# Path to Firebase service account credentials
# In Cloud Run, this will be mounted from Secret Manager
FIREBASE_CREDENTIALS_PATH=/app/firebase-credentials.json

# ============================================
# APPLICATION SETTINGS
# ============================================
ENVIRONMENT=production
DEBUG=False

# ============================================
# OPTIONAL: EMAIL CONFIGURATION
# ============================================
# If using email features (password reset, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@solacesquad.com

# ============================================
# OPTIONAL: CUSTOM DOMAIN
# ============================================
# Your custom domain (if configured)
# DOMAIN=app.solacesquad.com

# ============================================
# NOTES
# ============================================
# 1. Never commit this file to git!
# 2. Use Secret Manager for sensitive values in production
# 3. Update DATABASE_URL with your actual Cloud SQL connection details
# 4. Generate a strong SECRET_KEY
# 5. Get Firebase credentials from Firebase Console
