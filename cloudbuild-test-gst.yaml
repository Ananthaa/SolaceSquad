# ─────────────────────────────────────────────────────────────────────────────
# Cloud Build — Run Staging GST API Integration Tests
# Usage: gcloud builds submit --config cloudbuild-test-gst.yaml --project abiding-idea-485817-k2
# ─────────────────────────────────────────────────────────────────────────────
steps:
  - name: 'python:3.11-slim'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        pip install -r backend/requirements.txt --quiet 2>&1
        python3 backend/test_staging_gst.py
    env:
      - 'DB_USER=Admin'
      - 'DB_PASSWORD=SoulSquad2024x'
      - 'DB_NAME=solacesquad_prod'
      - 'INSTANCE_CONNECTION_NAME=abiding-idea-485817-k2:us-central1:solacesquad-login-data1'

timeout: 180s
logsBucket: 'gs://abiding-idea-485817-k2-build-logs'
