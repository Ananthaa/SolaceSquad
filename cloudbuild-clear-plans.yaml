# ─────────────────────────────────────────────────────────────────────────────
# Cloud Build — Clear all subscription plans from Mirror DB
# Usage: gcloud builds submit --config cloudbuild-clear-plans.yaml
#        --project abiding-idea-485817-k2 --no-source
# ─────────────────────────────────────────────────────────────────────────────
steps:
  - name: 'python:3.11-slim'
    entrypoint: bash
    args:
      - '-c'
      - |
        pip install sqlalchemy pg8000 "cloud-sql-python-connector[pg8000]" --quiet
        python backend/clear_plans.py
    env:
      - 'DB_USER=Admin'
      - 'DB_PASSWORD=SoulSquad2024x'
      - 'DB_NAME=solacesquad_mirror'
      - 'INSTANCE_CONNECTION_NAME=abiding-idea-485817-k2:us-central1:solacesquad-login-data1'

logsBucket: 'gs://abiding-idea-485817-k2-build-logs'
timeout: 300s
